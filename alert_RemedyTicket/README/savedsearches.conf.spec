# RemedyTicket alert action settings

action.RemedyTicket = [0|1]
* Enable RemedyTicket action

action.RemedyTicket.param.TicketSummary = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.

action.RemedyTicket.param.TicketNotes = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.

action.RemedyTicket.param.Priority = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.

action.RemedyTicket.param.TroubleSubject = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.

action.RemedyTicket.param.AssigneeArea = <string>
* URL to send the HTTP POST request to. Must be accessible from the Splunk server.
